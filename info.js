const params=new URLSearchParams(location.search),id=params.get('id'),item=items.find(x=>x.id===id),page=document.getElementById('infoPage');
if(!item){page.innerHTML='<div class="no-results" style="display:block"><h2>Item Not Found</h2><p>The requested page does not exist.</p><br><a class="back-btn" href="index.html">← Back Home</a></div>'}else{document.title=`${item.title} - MediaHub`;page.innerHTML=`<div class="info-container"><img class="info-poster" src="${item.image}" alt="${item.title}"><div class="info-details"><h1>${item.title}</h1><div class="info-meta"><span class="badge">${item.genre}</span><span class="badge">${item.year}</span><span class="badge">${item.status}</span></div><p>${item.description}</p><a class="back-btn" href="index.html">← Back</a></div></div>`}
const input=document.getElementById('searchInput');document.getElementById('searchBtn').onclick=()=>{const q=input.value.trim();location.href=q?`index.html?search=${encodeURIComponent(q)}`:'index.html'};document.getElementById('menuBtn').onclick=()=>document.getElementById('navMenu').classList.toggle('active');
